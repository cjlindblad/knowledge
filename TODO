Main
- Filter list based on prompt
    - Handle weird re behaviour (search for '[]')
    - Typing 'cse' should'nt match 'CSS'
    - Match words out of order

Polish
- Keep track of main window's width and height
- Don't allow prompt input on more than one line
- Don't allow non textual prompt input, like arrow key codes
