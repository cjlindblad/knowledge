TODO
- Really delete items (+ remove unused categories when doing so)
- Simplify knowledge item creation
- Clear deleted items after a given period (?)
- Trash non used categories
- List search term highlighting
- Don't allow prompt input on more than one line
- db config (path in env?)
- Don't save new item if nothing is added to text file
- UTC?